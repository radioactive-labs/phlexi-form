# frozen_string_literal: true

RSpec.describe Phlexi::Form do
  it "has a version number" do
    expect(Phlexi::Form::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
